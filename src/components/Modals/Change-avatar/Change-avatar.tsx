import ModalWrapper from "@templates/Modal-wrapper";
import { FC } from "react";
import { ModalProps } from "@interfaces/modal";

const ChangeAvatar: FC<ModalProps> = ({ ...props }) => {
  return (
    <ModalWrapper {...props}>
      
    </ModalWrapper>
  )
}